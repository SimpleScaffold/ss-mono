#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# 로컬 hook은 개발 편의용 (soft-fail)
# node가 없거나 스크립트가 실패해도 커밋을 막지 않습니다.
if command -v node >/dev/null 2>&1; then
    node scripts/hooks/pre-commit.js || exit 0
else
    # GitHub Desktop 등 node가 PATH에 없는 경우 조용히 넘어갑니다.
    exit 0
fi
